# API Environment Configuration
# Copy to .env and fill in your values for local development

# Node Environment
NODE_ENV=development

# Claude API Configuration
# Get your Claude API key from: https://console.anthropic.com/
CLAUDE_API_KEY=sk-ant-your-api-key-here
CLAUDE_API_URL=https://api.anthropic.com/v1/messages
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# AWS Secrets Manager Configuration (for production)
# Set to 'true' to use AWS Secrets Manager instead of CLAUDE_API_KEY
AWS_SECRETS_ENABLED=false
CLAUDE_SECRET_NAME=prod/proto-portal/claude-api-key
AWS_REGION=us-east-1

# Server Configuration
PORT=3003

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:8082

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_SKIP_SUCCESSFUL_REQUESTS=true

# Security
JWT_SECRET=your-jwt-secret-here
API_KEY_SALT=your-api-key-salt-here

# Logging
LOG_LEVEL=debug

# Development Notes:
# 1. In local development (.env file):
#    - Set CLAUDE_API_KEY to your actual Claude API key
#    - Set AWS_SECRETS_ENABLED=false
#    - The API will use the CLAUDE_API_KEY directly
#
# 2. In production (AWS deployment):
#    - Set AWS_SECRETS_ENABLED=true
#    - Configure CLAUDE_SECRET_NAME and AWS_REGION
#    - The API will retrieve the key from AWS Secrets Manager
#    - Do not set CLAUDE_API_KEY in production environment
#
# 3. Mock Mode (no API key):
#    - Don't set CLAUDE_API_KEY
#    - Set AWS_SECRETS_ENABLED=false
#    - The API will run in mock mode for development/testing