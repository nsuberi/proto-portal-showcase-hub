# Environment Configuration Sample
# Copy this file to .env and fill in your actual values

# Claude AI Configuration
# For local development, set your Claude API key here
# Get your API key from https://console.anthropic.com/settings/keys
CLAUDE_API_KEY=sk-ant-api03-your-actual-api-key-here

# AWS Configuration (for local development that uses AWS Secrets Manager)
# Optional: Only needed if you want to test AWS Secrets Manager locally
# AWS_REGION=us-east-1
# CLAUDE_SECRET_NAME=prod/proto-portal/claude-api-key

# AWS Secrets Manager Control
# Set to 'false' to use local CLAUDE_API_KEY instead of AWS Secrets Manager
# Set to 'true' to use AWS Secrets Manager (requires AWS credentials configured)
AWS_SECRETS_ENABLED=false

# Claude API Configuration (optional overrides)
# CLAUDE_API_URL=https://api.anthropic.com/v1/messages
# CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Development Environment
NODE_ENV=development

# API Server Configuration
# The API server runs on port 3003 by default (to avoid conflicts with frontend apps)
# You can override this with PORT=3003 if needed
PORT=3003