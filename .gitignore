# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Yarn Berry
.yarn/install-state.gz

playwright-report/*

# Environment variables
**/.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python virtual environments
.lint-venv/
.venv/
*venv*/

# Terraform state
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Terraform build artifacts
terraform/*.zip
terraform/modules/**/*.zip
# Terraform plan files may contain secrets (creds, tokens, sensitive vars) — never commit.
terraform/tfplan
terraform/tfplan.*
**/tfplan
**/tfplan.*
*.tfplan

# Playwright test output
test-results/

# PDF.js vendor binary character maps
*.bcmap

# Vendored static assets (downloaded dependencies, not source)
apps/ai-evals-in-context/ai-testing-resource/static/vendor/

# Local credential bootstrap workspace — never commit
z_creds/
